# 🎰 Специфікація ігор AURA Casino

Детальна специфікація дизайну та механік для кожної гри в екосистемі **AURA Casino**. Усі ігри розроблені з урахуванням концепції **Digital Luxury**: глибокий темно-синій фон, неонові акценти, ефекти скла (glassmorphism) та тактильний відгук.

---

## 1. 🚀 Crash (Політ у Неон)

Це гра на витримку. Гравець робить ставку і спостерігає, як зростає множник. Потрібно встигнути забрати гроші до того, як ракета вибухне.

### Дизайн та Візуал:

* **Центральний елемент:** Мінімалістична неонова ракета або стилізована "світлова лінія" (як у фільмі Tron), що рухається вгору по параболі.
* **Фон:** Динамічна сітка (grid), яка створює відчуття швидкості та глибини космосу.
* **Колірна схема:** Починається з спокійного бірюзового (`#00FFFF`), на позначці 2.0x переходить у фіолетовий, а вище 10.0x — у яскраво-червоне неонове світлиння.
* **Анімація:** При вибуху (Crash) екран на мить спалахує білим, ракета розлітається на пікселі, спрацьовує сильний вібровідгук (Haptic).

### Механіка:

* **Двигун:** Сервер генерує випадкове число (множник) за алгоритмом SHA-256 ще до початку раунду.
* **Авто-вивід:** Гравець може встановити значення (напр. 1.5x), при якому система забере виграш автоматично.
* **Live-статистика:** Збоку відображаються ставки інших гравців у реальному часі ("Вийшов на 2.3x", "Програв").

---

## 2. 💣 Mines (Скарбниця Неону)

Класична гра, де потрібно відкривати клітинки на полі 5х5, уникаючи мін. Чим більше зірок знайдено — тим вищий множник.

### Дизайн та Візуал:

* **Поле:** Сітка з 25 квадратів з ефектом матового скла.
* **Стан клітинок:** 
  * *Закрита:* Легкий неоновий контур.
  * *Зірка:* Золотий кристал з променями світла.
  * *Міна:* Червона геометрична сфера, що пульсує.
* **Ефекти:** При відкритті зірки звучить кришталевий дзинь. При попаданні на міну — звук глухого вибуху і "тріщина" на склі інтерфейсу.

### Механіка:

* **Налаштування:** Гравець сам обирає кількість мін (від 1 до 24). Це змінює ризик та прогресію множника.
* **Cashout:** Можливість забрати гроші після кожного успішного кроку.
* **Provably Fair:** Після гри юзер отримує "Seed", за допомогою якого може перевірити, що міни не були переставлені під час гри.

---

## 3. 🎲 Dice (Цифровий Шанс)

Гра на вгадування діапазону чисел від 0 до 100.

### Дизайн та Візуал:

* **Інтерфейс:** Горизонтальний слайдер з градієнтом. Ліва частина — червона (програш), права — зелена (виграш).
* **Контролер:** Неонова кулька, яку гравець перетягує, змінюючи шанс на перемогу (напр. "Більше 40").
* **Результат:** Число з'являється у великому скляному вікні з ефектом розмиття фону.

### Механіка:

* **Шанс vs Множник:** Чим менший шанс на перемогу (вужчий діапазон), тим вищий коефіцієнт виплати.
* **Миттєвий розрахунок:** Результат генерується миттєво після натискання кнопки "Roll".

---

## 4. ⚔️ Telegram Battle (PvP CoinFlip)

Дуель між двома гравцями на вибір сторони монети.

### Дизайн та Візуал:

* **Екран очікування:** Два слоти для аватарок гравців. Між ними — неонова лінія розлому.
* **Монета:** Двостороння 3D-монета. Одна сторона — логотип AURA (фіолетовий), інша — символ TON (блакитний).
* **Анімація:** Монета підлітає вгору, обертаючись у сповільненій зйомці (slow-motion), створюючи напругу.

### Механіка:

* **Створення дуелі:** Гравець створює ставку. Інший бачить її в лобі та приймає виклик.
* **Розподіл:** Переможець забирає 95% банку, 5% — комісія платформи.

---

## 🃏 Blackjack / Дурак (PvP Card Games)

Класичні карткові ігри в сучасному виконанні.

### Дизайн та Візуал:

* **Стіл:** Віртуальне сукно темно-синього кольору з геометричними візерунками.
* **Карти:** Дизайн "Cyber-Deck". Замість звичайних малюнків — неонові позначення мастей. Сорочка карти — переливчастий градієнт.
* **Інтерфейс:** Кнопки "Взяти", "Пас", "Бити" з'являються плавно знизу, мають великий розмір для зручності гри великим пальцем.

### Механіка:

* **Real-time:** Повна синхронізація через WebSockets. Гравці бачать рух карт суперника.
* **Таймер:** На кожен хід дається 20 секунд. Якщо час вийшов — автоматичний фол.

---

## 💎 Загальні елементи UX для всіх ігор:

* **Верхня панель:** Завжди відображає поточний баланс, який "перетікає" (анімовано змінюється цифра) після кожного виграшу.
* **Live Feed:** У нижній частині кожної гри йде тонка стрічка з останніми виграшами інших користувачів: `User123 win 45.0 USDT in Mines 🔥`.
* **Sound Design:** М'який Ambient на фоні та чіткі механічні звуки кнопок.

---

## 🏗 Структура бази даних для PvP

### 1. Таблиця `Rooms` (Кімнати)

Ця таблиця зберігає загальну інформацію про ігрову сесію.

| Поле | Тип | Опис |
| --- | --- | --- |
| `id` | UUID | Унікальний ідентифікатор кімнати. |
| `game_type` | String | Тип гри (`durak`, `coinflip`, `poker`). |
| `bet_amount` | Decimal | Ставка на гру. |
| `currency` | String | Валюта (`USDT`, `TON`). |
| `status` | String | `waiting` (очікування), `playing` (у процесі), `finished`. |
| `is_private` | Boolean | Чи доступна кімната за посиланням. |
| `password` | String | Хеш пароля (якщо приватна). |
| `created_at` | Timestamp | Час створення. |

### 2. Таблиця `Game_State` (Стан гри)

Тут зберігається все, що відбувається "всередині" колоди.

| Поле | Тип | Опис |
| --- | --- | --- |
| `room_id` | UUID | Зв'язок з таблицею кімнати. |
| `deck` | JSONB | Масив карт, що залишилися в колоді. |
| `trump_card` | JSONB | Козирна карта (для Дурака). |
| `table_cards` | JSONB | Карти, що лежать на столі (атака/захист). |
| `turn_user_id` | BigInt | Telegram ID гравця, чий зараз хід. |
| `turn_deadline` | Timestamp | Час, до якого гравець має зробити хід (таймер). |
| `history` | JSONB | Лог усіх ходів (для аналітики та вирішення спорів). |

### 3. Таблиця `Game_Participants` (Учасники)

Зв'язок гравців з кімнатою.

| Поле | Тип | Опис |
| --- | --- | --- |
| `room_id` | UUID | Зв'язок з кімнатою. |
| `user_id` | BigInt | Telegram ID гравця. |
| `hand` | JSONB | Карти, які зараз на руках у цього гравця (приховано від інших). |
| `position` | Integer | Порядок місця за столом (1, 2, 3...). |
| `is_ready` | Boolean | Чи підтвердив гравець готовність до старту. |

---

## ⚡ Логіка реального часу (WebSockets & Redis)

Для PvP не підходить звичайна база даних для кожного кліку — це занадто повільно. Використовуємо **Redis** як проміжний шар.

### Подія "Хід гравця":

1. Клієнт відправляє через Socket.io: `{ "action": "attack", "card": "Ace_Hearts" }`.
2. Сервер перевіряє:
   - Чи зараз хід цього юзера?
   - Чи є у нього ця карта в `hand` (в Redis)?
   - Чи відповідає хід правилам гри?
3. Якщо все ок: Сервер оновлює стан в Redis і розсилає всім іншим гравцям у кімнаті оновлений `table_cards`.

### Захист від вильоту (Reconnection):

* Якщо юзер оновив Mini App, фронтенд робить запит `getGameState`.
* Сервер витягує з бази/Redis поточні карти на руках цього юзера і стан столу.
* Гравець миттєво повертається в ту саму секунду гри.

---

## 🛡 Безпека PvP

* **Server-Side Logic Only:** Клієнт ніколи не знає, які карти в колоді або у суперника. Сервер надсилає в `hand` суперника лише масив типу `[hidden, hidden, hidden]`.
* **Validation:** Кожна дія перевіряється сервером. Неможливо "викинути" карту, якої немає, навіть якщо зламати фронтенд-код.
* **Escrow System:** Гроші списуються з обох гравців **у момент старту** гри. Вони зберігаються в "буфері" (escrow) і нараховуються переможцю лише після фінального сигналу сервера.

---

## 💸 Комісія (Rake)

В адмін-панелі має бути поле: `PvP_Commission_Percent`.

**Приклад розрахунку:**

* Ставка: 10 USDT з кожного. Загальний банк: 20 USDT.
* Комісія: 5%.
* Переможець отримує: `20 - (20 * 0.05) = 19 USDT`.
* Дохід AURA: `1 USDT`.

---

## 🏗 Система Створення Лобі та Пошуку Гравців

Це "передпокій" перед грою. Система має бути швидкою та іншою для кожної гри.

### Механіка створення:

1. **Public Matchmaking:** Гравець обирає гру та ставку. Система шукає кімнату з такою ж ставкою. Якщо не знаходить протягом 10 сек — пропонує створити власну.
2. **Private Table:** Гравець створює стіл, обирає "Private" і отримує **Invite Code** або пряме посилання в Telegram.
3. **Chat у Лобі:** До того, як гра почалася, гравці можуть обмінюватися швидкими фразами: *"Good luck!"*, *"Ready?"*, *"High stakes only"*.

---

## 🃏 Алгоритм гри в "Дурака" (PvP)

Для реалізації розробник повинен використовувати **State Machine** на сервері.

### Процес:

* **Роздача:** Сервер перемішує масив карт (36 шт). Кожному гравцю відправляє масив з 6 об'єктів. Решта — у колоду (Redis).
* **Козир:** Остання карта колоди стає козирем. Її `suit` (масть) записується в стан кімнати.
* **Хід:**
  1. Перевірка: чи є карта у гравця? чи підходить вона за мастю/рангом?
  2. Подія `card_played`: сервер оновлює стан столу для обох гравців.
* **Фінал:** Коли в одного гравця 0 карт, а колода порожня — сервер оголошує переможця, списує комісію (Rake) і нараховує виграш.

---

## ♠️ Система Онлайн Покеру (Texas Hold'em)

Потребує складнішої математики для визначення комбінацій.

### Етапи раунду:

1. **Pre-flop:** Роздача 2 карт кожному.
2. **Flop / Turn / River:** Поступове відкриття 5 спільних карт на столі.
3. **Betting Rounds:** Гравці мають опції `Fold` (скинути), `Check` (пропустити), `Call` (зрівняти), `Raise` (підняти).
4. **Showdown:** Сервер порівнює найкращі комбінації з 5 карт.

> **Важливо:** Для покеру потрібно реалізувати **Time-Bank**. Якщо гравець довго думає, у нього є додаткові 30 секунд на всю партію, щоб не затримувати інших.

---

## 💬 Система Чатів та Соціалізації

Щоб ігри не були нудними, додаємо інтерактив:

* **Live Game Chat:** Вікно чату з правого боку або знизу.
* **Реакції (Emojis):** Можливість "кинути" в опонента анімований емодзі (помідор, вогонь, лайк), що з'являється поверх його аватарки на 2 секунди.
* **Quick Phrases:** Набір готових фраз на 6 мовах, які автоматично перекладаються для опонента (ти пишеш англійською, а китаєць бачить китайською).

---

## ⚙️ Повний технічний цикл гри (Backend Flow)

Ось як це бачить програміст:

1. **User A** створює кімнату → Запис у **Redis** (ключ: `room:123:state`).
2. **User B** заходить → Подія `room_joined` через **Socket.io**.
3. **Старт** → Списання балансу (Database Transaction) → Статус `playing`.
4. **Кожен хід** → Валідація на сервері (Server-side validation) → Оновлення Redis → Broadcast іншим гравцям.
5. **Кінець** → Розрахунок виграшу → Запис у **PostgreSQL** (History) → Видалення кімнати з Redis.

---

## 📝 Технічні вимоги для розробника:

> "Потрібно реалізувати **Game Engine** на базі Node.js, який буде ізольованим від фронтенду. Вся логіка (рахунок очок у покері, правила Дурака) має бути на 100% серверною. Фронтенд — це лише візуалізатор (React). Використовувати **Redis Streams** для обробки подій у реальному часі."

### Стек технологій:

* **Backend:** Node.js + Express + Socket.io
* **База даних:** SQLite (поточна) / PostgreSQL (для PvP)
* **Кеш:** Redis (для стану ігор)
* **Frontend:** React + Vite
* **Provably Fair:** SHA-256 для генерації випадкових чисел

---

## 🎨 Дизайн-система

### Кольори:

* **Фон:** `#0A0E27` (глибокий темно-синій)
* **Неон бірюзовий:** `#00FFFF`
* **Неон фіолетовий:** `#9D4EDD`
* **Неон червоний:** `#FF006E`
* **Золотий:** `#FFD700`
* **Скло (glassmorphism):** `rgba(255, 255, 255, 0.1)` з `backdrop-filter: blur(10px)`

### Шрифти:

* **Заголовки:** Sans-serif, жирний, з неоновим світінням (`text-shadow`)
* **Текст:** Легкий, читабельний, контрастний

### Анімації:

* **Переходи:** `transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1)`
* **Hover ефекти:** Легке підсвічування та масштабування
* **Вібро:** Haptic feedback для важливих подій (виграш, програш)
