# Виправлення помилки "Not Found" після деплою

## Проблема
Після деплою отримується помилка `{"error":"Not Found"}` при спробі авторизації.

## Причини
1. **Frontend не скопійовано в public**: Dockerfile копіює frontend build, але потрібно переконатися, що це працює
2. **Неправильний шлях до index.html**: В логах видно помилку `/public/index.html` замість `/app/public/index.html`

## Виправлення

### 1. Додано логування в server.js
- Логування шляху до public директорії
- Логування шляху до index.html
- Детальні помилки при обробці

### 2. Перевірка Dockerfile
Dockerfile правильно копіює frontend:
```dockerfile
COPY --from=frontend-builder /app/frontend/dist ./public
```

Це копіює `frontend/dist/*` в `/app/public/` в контейнері.

### 3. Перевірка шляхів
В server.js:
- `__dirname` = `/app/backend` (в контейнері)
- `join(__dirname, '../public')` = `/app/public` ✅

## Наступні кроки

1. **Перезадеплойте:**
```bash
fly deploy --app auraslots
```

2. **Перевірте логи:**
```bash
fly logs --app auraslots
```

Маєте побачити:
- `📁 Public directory: /app/public`
- `📄 Serving index.html from: /app/public/index.html`

3. **Якщо все ще помилка:**
   - Перевірте, чи існує файл в контейнері:
   ```bash
   fly ssh console --app auraslots
   ls -la /app/public/
   ```

## Додаткова діагностика

Якщо проблема залишається, перевірте:

1. **Чи правильно зібрано frontend:**
```bash
cd frontend
npm run build
ls -la dist/
```

2. **Чи скопійовано в public локально:**
```bash
# З директорії frontend
Copy-Item -Path dist\* -Destination ..\public\ -Recurse -Force
```

3. **Перевірте структуру public:**
```bash
ls -la public/
# Має бути: index.html, assets/, materials/
```

## Очікуваний результат

Після виправлень:
- ✅ Логи показують правильні шляхи
- ✅ index.html знаходиться та обслуговується
- ✅ Авторизація працює
- ✅ Frontend завантажується
