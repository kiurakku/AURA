# 🔧 Виправлення проблеми з встановленням

## Проблема
`better-sqlite3` потребує компіляції нативних модулів, для чого потрібні Visual Studio Build Tools на Windows.

## Рішення
Замінено `better-sqlite3` на JSON файлову систему, яка не потребує компіляції.

## Що змінено

1. **backend/package.json** - видалено `better-sqlite3` та `sql.js`
2. **backend/database/db.js** - переписано для використання JSON файлів
3. **backend/database/db-simple.js** - створено просту JSON базу даних
4. Всі файли оновлено для роботи з новою базою даних

## Встановлення

Тепер просто виконайте:

```bash
npm run install:all
```

Або окремо:

```bash
cd backend && npm install
cd ../frontend && npm install
```

## Як працює нова база даних

- Дані зберігаються в JSON файлах у папці `backend/data/`
- Файли: `users.json`, `transactions.json`, `games.json`, `referrals.json`, `settings.json`
- Не потребує компіляції
- Працює на всіх платформах
- Автоматичне збереження при кожній операції

## Переваги

✅ Не потребує Visual Studio Build Tools
✅ Працює на Windows, Linux, macOS
✅ Швидке встановлення
✅ Легко резервувати (просто скопіювати JSON файли)

## Недоліки

⚠️ Менш ефективна для великих обсягів даних
⚠️ Немає транзакцій (але для цього проекту це не критично)

## Для продакшн

Якщо потрібна більша продуктивність, можна:
1. Встановити Visual Studio Build Tools
2. Повернутися до `better-sqlite3`
3. Або використати PostgreSQL/MySQL

Але для початку JSON файли цілком достатньо!
