# 🛡️ Адмін Панель AURA Casino

## 📋 Зміст
- [Як зайти](#як-зайти)
- [Функціонал](#функціонал)
- [API Endpoints](#api-endpoints)
- [Налаштування](#налаштування)

## 🔐 Як зайти

### Крок 1: Налаштування адміністраторів

Додайте ваш Telegram ID до змінної середовища `ADMIN_IDS` у файлі `.env`:

```env
ADMIN_IDS=123456789,987654321
```

**Як дізнатися свій Telegram ID:**
1. Напишіть боту [@userinfobot](https://t.me/userinfobot)
2. Або використайте команду `/start` у вашому боті та перевірте логи сервера

### Крок 2: Доступ до адмін панелі

Адмін панель доступна через API endpoints. Всі запити вимагають авторизації через Telegram WebApp `initData`.

**Приклад доступу через браузер/Postman:**

1. Відкрийте Telegram WebApp вашого бота
2. Відкрийте DevTools (F12) → Console
3. Виконайте: `window.Telegram.WebApp.initData`
4. Скопіюйте отриманий рядок
5. Використовуйте його в заголовку `x-telegram-init-data` для всіх запитів

**Приклад запиту:**

```bash
curl -X GET "http://localhost:3001/api/admin/dashboard" \
  -H "x-telegram-init-data: YOUR_INIT_DATA_HERE"
```

## 🎯 Функціонал

### 📊 Dashboard (Статистика)

**Endpoint:** `GET /api/admin/dashboard`

Показує загальну статистику платформи:
- Загальна кількість користувачів
- Активні користувачі (за останні 7 днів)
- Загальна кількість ігор
- Загальна сума ставок (Total Wagered)
- Загальна сума виграшів (Total Won)
- Загальна сума депозитів
- Загальна сума виводів
- Очікуючі виводи (Pending Withdrawals)
- Gross Gaming Revenue (GGR)
- Net Revenue

**Приклад відповіді:**

```json
{
  "stats": {
    "totalUsers": 150,
    "activeUsers": 45,
    "totalGames": 1250,
    "totalWagered": 50000,
    "totalWon": 47500,
    "totalDeposits": 10000,
    "totalWithdrawals": 5000,
    "pendingWithdrawals": 2000,
    "grossGamingRevenue": 2500,
    "netRevenue": 5000
  }
}
```

### 👥 Управління користувачами

#### Отримати список всіх користувачів

**Endpoint:** `GET /api/admin/users`

Отримує список всіх користувачів з повною інформацією:
- Telegram ID
- Username та First Name
- Баланс та бонусний баланс
- Реферальний код
- Дата реєстрації
- Остання активність
- Ранг та XP

**Приклад відповіді:**

```json
{
  "users": [
    {
      "id": 1,
      "telegram_id": 123456789,
      "username": "user123",
      "first_name": "John",
      "balance": 100.50,
      "bonus_balance": 10.00,
      "referral_code": "abc123",
      "created_at": "2024-01-01T00:00:00.000Z",
      "rank_name": "Gambler",
      "total_wagered": 500
    }
  ]
}
```

#### Отримати інформацію про конкретного користувача

**Endpoint:** `GET /api/admin/users/:userId`

Отримує детальну інформацію про користувача, включаючи транзакції та ігри.

**Параметри:**
- `userId` - ID користувача або Telegram ID

**Приклад відповіді:**

```json
{
  "user": {
    "id": 1,
    "telegram_id": 123456789,
    "username": "user123",
    "balance": 100.50,
    "bonus_balance": 10.00
  },
  "transactions": [ ... ],
  "games": [ ... ]
}
```

#### Пошук користувачів

**Endpoint:** `GET /api/admin/users/search/:query`

Шукає користувачів за Telegram ID, username або first_name.

**Приклад запиту:**

```bash
curl -X GET "https://auraslots.fly.dev/api/admin/users/search/user123" \
  -H "x-telegram-init-data: YOUR_INIT_DATA"
```

#### Нарахування балансу користувачу

**Endpoint:** `POST /api/admin/users/:userId/balance`

Дозволяє адміністратору нарахувати баланс (основний або бонусний) конкретному користувачу.

**Параметри:**
- `userId` (URL) - ID користувача або Telegram ID
- `amount` (body) - Сума для нарахування (обов'язково)
- `currency` (body) - Валюта (за замовчуванням: 'USDT')
- `type` (body) - Тип балансу: 'balance' (основний) або 'bonus' (бонусний) (за замовчуванням: 'balance')
- `description` (body) - Опис транзакції (опціонально)

**Приклад запиту:**

```bash
curl -X POST "https://auraslots.fly.dev/api/admin/users/123456789/balance" \
  -H "x-telegram-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 10.5,
    "currency": "USDT",
    "type": "balance",
    "description": "Бонус за активність"
  }'
```

**Приклад нарахування бонусного балансу:**

```bash
curl -X POST "https://auraslots.fly.dev/api/admin/users/123456789/balance" \
  -H "x-telegram-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 5,
    "currency": "USDT",
    "type": "bonus",
    "description": "Промо-бонус"
  }'
```

**Приклад відповіді:**

```json
{
  "success": true,
  "message": "Balance updated successfully",
  "user": {
    "id": 1,
    "telegram_id": 123456789,
    "username": "user123",
    "balance": 111.0,
    "bonus_balance": 5.0
  }
}
```

**Примітки:**
- Всі операції логуються в транзакції з типом `admin_bonus`
- Зберігається інформація про адміністратора, який виконав операцію
- `userId` може бути як внутрішнім ID, так і Telegram ID

### 💰 Управління виводами

#### Отримати список виводів

**Endpoint:** `GET /api/admin/withdrawals?status=pending`

Параметри:
- `status` - статус виводу: `pending`, `completed`, `rejected` (за замовчуванням: `pending`)

**Приклад відповіді:**

```json
{
  "withdrawals": [
    {
      "id": 1,
      "user_id": 1,
      "type": "withdrawal",
      "amount": 50.00,
      "currency": "USDT",
      "status": "pending",
      "address": "TXYZ123...",
      "created_at": "2024-01-15T10:00:00.000Z"
    }
  ]
}
```

#### Підтвердити вивід

**Endpoint:** `POST /api/admin/withdrawals/:id/approve`

Підтверджує вивід та змінює статус на `completed`.

**Приклад запиту:**

```bash
curl -X POST "http://localhost:3001/api/admin/withdrawals/1/approve" \
  -H "x-telegram-init-data: YOUR_INIT_DATA"
```

**Приклад відповіді:**

```json
{
  "success": true,
  "message": "Withdrawal approved"
}
```

#### Відхилити вивід

**Endpoint:** `POST /api/admin/withdrawals/:id/reject`

Відхиляє вивід, повертає кошти на баланс користувача та змінює статус на `rejected`.

**Body:**
```json
{
  "reason": "Недостатньо коштів на рахунку"
}
```

**Приклад запиту:**

```bash
curl -X POST "http://localhost:3001/api/admin/withdrawals/1/reject" \
  -H "x-telegram-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{"reason": "Недостатньо коштів"}'
```

**Приклад відповіді:**

```json
{
  "success": true,
  "message": "Withdrawal rejected"
}
```

### 🏆 Leaderboard (Таблиця лідерів)

**Endpoint:** `GET /api/admin/leaderboard?period=all&limit=10`

Параметри:
- `period` - період: `all`, `week`, `day` (за замовчуванням: `all`)
- `limit` - кількість записів (за замовчуванням: 10)

**Приклад відповіді:**

```json
{
  "leaderboard": [
    {
      "position": 1,
      "user_id": 1,
      "username": "user123",
      "first_name": "John",
      "photo_url": "https://...",
      "total_won": 5000,
      "rank": "High Roller"
    }
  ],
  "period": "all"
}
```

## ⚙️ Налаштування

### Змінні середовища

Додайте до файлу `.env`:

```env
# Список Telegram ID адміністраторів (через кому)
ADMIN_IDS=123456789,987654321

# Токен Telegram бота
TELEGRAM_BOT_TOKEN=your_bot_token_here

# URL WebApp
TELEGRAM_WEBAPP_URL=https://your-domain.com
```

### Безпека

⚠️ **Важливо:**
- Ніколи не публікуйте `ADMIN_IDS` у публічних репозиторіях
- Використовуйте HTTPS для продакшн середовища
- Регулярно перевіряйте логи на підозрілу активність
- Обмежте доступ до адмін endpoints за IP (опціонально)

## 🔧 Розширення функціоналу

### Майбутні функції (в розробці):

1. **Управління RTP (Return to Player)**
   - Налаштування відсотка віддачі для кожної гри
   - Глобальні та індивідуальні налаштування

2. **Анти-фрод система**
   - Виявлення підозрілої активності
   - Автоматичне блокування мульти-акаунтів

3. **CMS (Content Management System)**
   - Управління банерами
   - Редагування повідомлень бота

4. **Ризик-менеджмент**
   - Налаштування лімітів ставок
   - Контроль балансів користувачів

## 📞 Підтримка

Якщо виникли питання або проблеми з доступом до адмін панелі:
1. Перевірте, чи ваш Telegram ID додано до `ADMIN_IDS`
2. Переконайтеся, що `initData` валідний та не застарів
3. Перевірте логи сервера на наявність помилок

---

**Версія:** 1.0.0  
**Останнє оновлення:** 2024
