# 🔧 Інструкція з налаштування

## Крок 1: Встановлення залежностей

```bash
# Встановити залежності для backend та frontend
npm run install:all
```

Або окремо:
```bash
cd backend && npm install
cd ../frontend && npm install
```

## Крок 2: Налаштування Backend

1. Створіть файл `backend/.env`:
```bash
cd backend
cp .env.example .env
```

2. Відредагуйте `backend/.env`:
```env
TELEGRAM_BOT_TOKEN=8220670161:AAEDEyB-efY37A-1Mj0OuzJKP1YMrXscLLI
TELEGRAM_BOT_USERNAME=your_bot_username
TELEGRAM_WEBAPP_URL=http://localhost:5173
PORT=3000
SECRET_KEY=your-secret-key-change-this
```

## Крок 3: Налаштування Telegram Bot

1. Відкрийте [@BotFather](https://t.me/BotFather) в Telegram
2. Створіть нового бота або використайте існуючого
3. Отримайте токен бота
4. Налаштуйте WebApp:
   ```
   /newapp
   ```
   - Виберіть вашого бота
   - Вкажіть назву: AURA Casino
   - Вкажіть опис: Найкраще казино в Telegram
   - Вкажіть URL: http://localhost:5173 (для розробки) або ваш домен (для продакшн)
   - Завантажте іконку (опціонально)

## Крок 4: Запуск

### Розробка

**Термінал 1 (Backend):**
```bash
cd backend
npm run dev
```

**Термінал 2 (Frontend):**
```bash
cd frontend
npm run dev
```

### Продакшн

**Backend:**
```bash
cd backend
npm start
```

**Frontend:**
```bash
cd frontend
npm run build
# Задеплойте папку dist на ваш хостинг
```

## Крок 5: Тестування

1. Відкрийте вашого бота в Telegram
2. Натисніть `/start`
3. Натисніть кнопку "Відкрити казино"
4. Перевірте всі функції

## 🔐 Налаштування безпеки

1. Змініть `SECRET_KEY` на випадковий рядок (мінімум 32 символи)
2. Для продакшн використовуйте HTTPS
3. Додайте адмінів у `ADMIN_USER_IDS` (через кому)

## 💰 Налаштування платежів

### CryptoPay API (опціонально)

1. Зареєструйтеся на [CryptoPay](https://crypt.bot)
2. Отримайте API ключ
3. Додайте у `backend/.env`:
```env
CRYPTOPAY_API_KEY=your-api-key
```

### TON Connect (опціонально)

Для інтеграції TON Connect потрібно:
1. Встановити `@tonconnect/sdk`
2. Налаштувати TON Connect маніфест
3. Додати обробку транзакцій

## 📊 База даних

База даних створюється автоматично при першому запуску в `backend/data/casino.db`.

Для скидання бази даних просто видаліть файл `backend/data/casino.db`.

## 🐛 Вирішення проблем

### Помилка авторизації
- Перевірте, чи правильно налаштований `TELEGRAM_BOT_TOKEN`
- Переконайтеся, що WebApp URL відповідає URL вашого фронтенду

### Помилки CORS
- Перевірте налаштування CORS у `backend/server.js`
- Для продакшн додайте ваш домен у whitelist

### База даних не створюється
- Перевірте права доступу до папки `backend/data`
- Переконайтеся, що SQLite встановлений

## 📞 Підтримка

Якщо виникли проблеми, перевірте:
1. Логи backend сервера
2. Консоль браузера (F12)
3. Налаштування `.env` файлу
