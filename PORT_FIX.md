# 🔧 Виправлення проблеми з портом

## Проблема
Порт 3000 вже зайнятий іншим процесом.

## Рішення
Змінено порт backend на **3001**.

## Що змінено

1. **backend/.env** - `PORT=3001`
2. **frontend/src/utils/api.js** - оновлено URL на `http://localhost:3001/api`

## Запуск

Тепер backend запуститься на порту **3001**:

```bash
cd backend
npm run dev
```

Frontend залишається на порту **5173** і автоматично підключиться до backend на порту 3001.

## Альтернативне рішення

Якщо хочете використовувати порт 3000, зупиніть процес, який його займає:

```bash
# Знайти процес
netstat -ano | findstr :3000

# Зупинити процес (замініть PID на номер з попередньої команди)
taskkill /PID 6900 /F
```

Але простіше використовувати порт 3001! ✅
