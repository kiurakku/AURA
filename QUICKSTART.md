# ⚡ Швидкий старт

## 1. Встановлення (1 хвилина)

```bash
# Встановити всі залежності
npm run install:all
```

## 2. Налаштування (2 хвилини)

### Backend
```bash
cd backend
# Файл .env вже створено з токеном бота
# Перевірте налаштування за потреби
```

### Frontend
```bash
cd frontend
# Налаштування не потрібні для розробки
```

## 3. Запуск (30 секунд)

**Термінал 1:**
```bash
cd backend
npm run dev
```

**Термінал 2:**
```bash
cd frontend
npm run dev
```

## 4. Тестування

1. Відкрийте Telegram
2. Знайдіть вашого бота (або створіть через @BotFather)
3. Натисніть `/start`
4. Натисніть "Відкрити казино"
5. Готово! 🎉

## 📝 Важливі налаштування

### Для продакшн:

1. **Змініть SECRET_KEY** у `backend/.env`
2. **Налаштуйте WebApp URL** через BotFather
3. **Використовуйте HTTPS** для продакшн
4. **Додайте адмінів** у `ADMIN_USER_IDS`

## 🎮 Доступні ігри

- 🚀 **Crash** - Вгадай момент виходу
- 🎲 **Dice** - Більше чи менше
- 💣 **Mines** - Знайди всі міни

## 💰 Платежі

Для реальних платежів налаштуйте:
- CryptoPay API (в `backend/.env`)
- Або TON Connect (потрібна додаткова інтеграція)

## 🐛 Проблеми?

1. Перевірте, чи запущений backend (порт 3000)
2. Перевірте, чи запущений frontend (порт 5173)
3. Перевірте токен бота у `.env`
4. Перевірте логи в консолі

## 📚 Детальна документація

Див. `SETUP.md` для повної інструкції.
